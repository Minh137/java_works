$(function(){
   $("#rid").change(function(){
   if($(this).is(":checked")){
   alert("PC방,학교 등 공공장소에서는 체크라지 마십시오.");
		}   
	});
	
	
	
	$("#loginform").validate({
    reules:{
        userid: "required",
        userpass:"required"
    },
    messages:{
        userid: "아이디를 입력하세요.",
        userpass: "아이디를 입력하세요."
    },
    showErrors: function(errorMap,errorList){
    //에러메시지
    if(errerList.length){
    	alert(errorList[0].message);
    }
    }
});
});

